namespace Calculator
{
    /// <summary>
    ///     Enum for token kinds.
    /// </summary>
    public enum TokenKind
    {
        EndOfSource,
        Number,
        Identifier,
        Comma,
        Plus,
        Minus,
        Asterisk,
        Slash,
        Caret,
        LeftParen,
        RightParen,
    }
}